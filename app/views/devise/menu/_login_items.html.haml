- if user_signed_in?
  %li= current_user.email
  %li= link_to(t('general.logout'), destroy_user_session_path, :method => 'delete')
- else
  %li= link_to(t('general.login'), new_user_session_path, :method => 'get')

