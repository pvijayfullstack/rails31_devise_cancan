= form_for @result_type do |f|
  -if @result_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@result_type.errors.count, "error")} prohibited this result_type from being saved:"
      %ul
        - @result_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :active
    = f.check_box :active
  .actions
    = f.submit 'Save'
