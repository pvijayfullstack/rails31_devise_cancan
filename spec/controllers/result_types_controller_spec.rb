require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe ResultTypesController do

  # This should return the minimal set of attributes required to create a valid
  # ResultType. As you add validations to ResultType, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end

  describe "GET index" do
    it "assigns all result_types as @result_types" do
      result_type = ResultType.create! valid_attributes
      get :index
      assigns(:result_types).should eq([result_type])
    end
  end

  describe "GET show" do
    it "assigns the requested result_type as @result_type" do
      result_type = ResultType.create! valid_attributes
      get :show, :id => result_type.id.to_s
      assigns(:result_type).should eq(result_type)
    end
  end

  describe "GET new" do
    it "assigns a new result_type as @result_type" do
      get :new
      assigns(:result_type).should be_a_new(ResultType)
    end
  end

  describe "GET edit" do
    it "assigns the requested result_type as @result_type" do
      result_type = ResultType.create! valid_attributes
      get :edit, :id => result_type.id.to_s
      assigns(:result_type).should eq(result_type)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new ResultType" do
        expect {
          post :create, :result_type => valid_attributes
        }.to change(ResultType, :count).by(1)
      end

      it "assigns a newly created result_type as @result_type" do
        post :create, :result_type => valid_attributes
        assigns(:result_type).should be_a(ResultType)
        assigns(:result_type).should be_persisted
      end

      it "redirects to the created result_type" do
        post :create, :result_type => valid_attributes
        response.should redirect_to(ResultType.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved result_type as @result_type" do
        # Trigger the behavior that occurs when invalid params are submitted
        ResultType.any_instance.stub(:save).and_return(false)
        post :create, :result_type => {}
        assigns(:result_type).should be_a_new(ResultType)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        ResultType.any_instance.stub(:save).and_return(false)
        post :create, :result_type => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested result_type" do
        result_type = ResultType.create! valid_attributes
        # Assuming there are no other result_types in the database, this
        # specifies that the ResultType created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        ResultType.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => result_type.id, :result_type => {'these' => 'params'}
      end

      it "assigns the requested result_type as @result_type" do
        result_type = ResultType.create! valid_attributes
        put :update, :id => result_type.id, :result_type => valid_attributes
        assigns(:result_type).should eq(result_type)
      end

      it "redirects to the result_type" do
        result_type = ResultType.create! valid_attributes
        put :update, :id => result_type.id, :result_type => valid_attributes
        response.should redirect_to(result_type)
      end
    end

    describe "with invalid params" do
      it "assigns the result_type as @result_type" do
        result_type = ResultType.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        ResultType.any_instance.stub(:save).and_return(false)
        put :update, :id => result_type.id.to_s, :result_type => {}
        assigns(:result_type).should eq(result_type)
      end

      it "re-renders the 'edit' template" do
        result_type = ResultType.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        ResultType.any_instance.stub(:save).and_return(false)
        put :update, :id => result_type.id.to_s, :result_type => {}
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested result_type" do
      result_type = ResultType.create! valid_attributes
      expect {
        delete :destroy, :id => result_type.id.to_s
      }.to change(ResultType, :count).by(-1)
    end

    it "redirects to the result_types list" do
      result_type = ResultType.create! valid_attributes
      delete :destroy, :id => result_type.id.to_s
      response.should redirect_to(result_types_url)
    end
  end

end
